export default class MarkerManager {
  constructor(map) {
    this.map = map;
    this.markers = [];
  }

  updateMarkers(spots) {
    console.log('time to update markers');
  }

  _spotsToAdd() {

  }

  _createMarkerFromSpot(spot) {

  }
};
